mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`)=video.title
        ul
            li #{videos.rating}/5.
            li #{videos.comment} Comments.
            li Posted #{videos.createdAt}.
            li #{videos.views} Views.